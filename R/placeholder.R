#' A placeholder function for the course package
#'
#' This is a tiny example function to ensure the repository can be used as
#' a package and passes basic checks when using devtools::check().
#'
#' @return A single string confirming the package is loaded.
#' @export
say_hello <- function() {
  "Hello from AI and Data Science for Transport package"
}
